# Flutter app (P09+ — not yet built)
Android + Web from one codebase. Generated Dio client from backend OpenAPI spec only —
never hand-written HTTP. l10n: ARB en + mr. Built on the dev machine (Flutter SDK not in sandbox).
