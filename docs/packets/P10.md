# Packet P10: master + config CRUD — DONE (Cowork, 2026-06-13)
Goal: admin/planning manage every master & config table the flows depend on; all writes audited.
Delivered:
- /master: materials, vendors (CRUD, search, no deletes anywhere — enforced by a test that
  walks ALL routes asserting no DELETE method exists), lines (+ line↔materials PUT),
  production-orders (CRUD), purchase-orders/boms/customers (read-only — SAP owns them).
- /config: reason-codes, splits (must total 100%, effective-dated, history immutable),
  mills, tolerances (§11.7), calendar (bulk month PUT), settings (ops_mode flip audited
  with who-did-it — playbook §7 human-checkpoint trail).
- Manual creates get source='manual' (§11.15) for importer reconciliation.
- apply_update(): allowed-field whitelist (sap_code immutable) + field-level audit diff
  in the same transaction.
Tests: 8 new — role matrix (vendor 403, plant_ops read-only), manual source, audit diff
(credit limit 14L→16.5L recorded before/after), immutable field rejection, split=100 rule,
settings audit, line-materials PUT, no-DELETE-routes guard. Suite: 33/33 verify-lite green.
Checklist: tests-first ✓ · scope ✓ · invariants ✓ · self-review ✓
Handoff: OpenAPI regenerated (auth+master+config now in spec → Flutter client gen in P09/P14).
Next: M2 inward — P15 files/photos, P16 gate entries, P17 scan decode (the 3 real invoices).
