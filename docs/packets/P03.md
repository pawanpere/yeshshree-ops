# Packet P03: full schema as models + alembic env — DONE (Cowork, 2026-06-11)
Goal: every Architecture §4 + §11 table as SQLAlchemy 2.0 models; alembic wired.
Delivered: 53 tables across 10 domain model files (base.py conventions: BigInt PKs, TEXT+CHECK enums,
JSONB/ARRAY with SQLite variants for verify-lite, Money/Qty, client_ref Uuid). alembic env reads DATABASE_URL.
Tests: tests/test_models.py — exact table-set assertion, CHECK rejection (role, approval status),
signed ledger roundtrip, vendors.source default. 8/8 green on SQLite.
Spec refinements (also patched into Architecture doc): stock_ledger.created_by NULL for system-written rows.
Agent-resolved ambiguities are logged in this packet's source agents' notes; key ones:
PO/production-order status = plain TEXT (no enum in spec); item_no Integer (SAP NUMC range);
doc_sequences.fiscal_year TEXT ("2025-26"); audit_log.user_id FK-less (append-only must never block).
Checklist: tests-first ✓ · scope ✓ · invariants ✓ (no enums, naming convention pinned) · self-review ✓
Verify: verify-lite green. NOT yet done (dev machine, HUMAN checkpoint): `make migration m="baseline"`
→ review generated file → `alembic upgrade head` → `alembic check` → commit migration.
Handoff: baseline migration is the next human action. Audit INSERT-only DB grant ships with the
migration packet (needs real PG; add `GRANT INSERT ON audit_log` + revoke UPDATE/DELETE in the
baseline migration's upgrade(), plus the postgres-marked test proving UPDATE fails).
