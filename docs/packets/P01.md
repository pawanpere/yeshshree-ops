# Packet P01: monorepo scaffold — DONE (Cowork, 2026-06-11)
Goal: `make repro` skeleton — repo structure, compose, Makefile, CLAUDE.md, FastAPI boots.
Delivered: full tree, docker-compose (pg16+minio+api+worker), Makefile (verify/verify-lite/seed/migration/openapi),
CLAUDE.md (12 invariants + never-do), conventions.md, ADR-001/002/003, .env.example, Dockerfile.
Checklist: tests-first ✓ (test_smoke written before main.py finalised) · scope ✓ · invariants ✓ · self-review ✓
Verify: verify-lite green in sandbox (8/8). Full `make repro` pending dev machine (docker).
Handoff: run Quick start in README; confirm /docs renders; healthz shows db:true against real PG.
