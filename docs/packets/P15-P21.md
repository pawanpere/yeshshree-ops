# Packets P15+P16+P17+P18+P19+P20+P21: M2 inward flow backend — DONE (Cowork, 2026-06-13)
Built by 3 parallel agents + shared core; integrated and full suite green.

## Shared core (pre-agent)
services/numbering.py (doc_sequences allocator, FY Apr–Mar, row-locked) ·
services/files.py (local/S3 driver, sha256 dedupe, non-blocking photos pattern) ·
services/anomaly.py (rule registry interface: run_rules/raise_hard/persist_soft/register_hard).

## P16+P18 gate entries (agent A — 14 tests)
Create (doc_type invoice/challan/other_inward, §11.5), duplicate-invoice 409 with consignment
continuation (§11.6), PO match: explicit / auto-when-exactly-one / unmatched; backfill +
complete-backfill (§11.3), attach-invoice (challan, allowed post-GR), link-PO + re-link while
open, mark-consumable, unmatched folder. No outbox (GR posts to SAP, gate doesn't).

## P17+P19 scan pipeline (agent B — 8 tests, golden vs the 3 REAL invoices)
PDF→pypdfium2→zxing-cpp; e-invoice JWS QR parse; TATA PDF417 (PO+vehicle+weight); dedupe-first;
vendor-by-GSTIN + open-PO resolution; phone decode-qr endpoint; agent heartbeat + station-status
with stale_seconds (§11.4). gate-agent/agent.py: stdlib watcher, move-after-200, retry-forever.
hsn_check='n/a' for MVP (mat_group is not an HSN — needs mapping table; TODO ref §11.18).

## P20+P21 goods receipt + anomalies (agent C — 9 tests)
One-transaction write path (invariant 3 proven): tolerance (§11.7, steel 0.5%) → anomaly rules
(hard ≥5× block w/ confirm-escalate path → registered + blocker notifications; soft >20% saved)
→ GR row → full-lot reject (§11.9: RGP doc, ZERO stock, debit draft) or GR_IN ledger + 5× debit
on shortage → outbox GR payload frozen → entry gr_done → audit. Anomaly register + resolve.

## Integration decisions
POST transactional routes return 200 (replay = identical result, invariant 4). Debit drafts have
approval_id NULL until P25 (approvals engine, M3). weighbridge_weight column NOT yet in
goods_receipts — needs model+migration packet (flagged, §11.18). Deps added: zxing-cpp,
pypdfium2, pillow, boto3. Suite: 64/64 verify-lite green. OpenAPI regenerated (45 endpoints).

Checklist: tests-first ✓ (all agents) · scope ✓ · invariants ✓ · self-review ✓ + integrator review ✓
Handoff: full verify on dev machine; gate-agent .exe packaging (pyinstaller) on Windows there.
Next: M3 (P23 stock/ops_mode, P24 issues+limits, P25 approvals, P26 SLA/delegates/override).
