# Packet P07: SAP seed importers + mock data — DONE (Cowork, 2026-06-11)
Goal: `make seed` loads the real plant-1117 exports + pilot config; golden-file tests lock the format.
Delivered:
- importers/sap_files.py — header-driven UTF-16 TSV parsers (materials, BOM, GRN report).
  Column-name → index mapping, so future SAP column reordering fails loudly, not silently.
- importers/mock.py — pilot config: 4 lines + production orders 100482-85, Bajaj customer,
  11 reason codes (en+mr), splits, mills, steel tolerance 0.5%, app settings, June-2026 calendar,
  8 demo users (demo1234/PIN 1234 — pilot onboarding replaces), vendor limit defaults.
- importers/seed.py — idempotent keyed upserts; --reset for dev; SQLite create_all in sandbox,
  Alembic owns DDL on Postgres. core/security.py — stdlib pbkdf2 (P04 builds on it).
Seeded from REAL data: 194 materials, 7 BOMs / 44 component lines (incl. negative scrap credits),
5 real vendors, 10 open POs derived from GRN history (real PO numbers + rates; quantities mock).
Tests: 4 golden-file + idempotency (run-twice ⇒ zero new rows). 12/12 green (verify-lite).
REAL-DATA CATCHES (all fed back into models/architecture doc):
1. mat_type CHECK dropped — real export has ERSA/HIBE/ROH1/LEIH/Z-types. Rule learned:
   never CHECK-constrain a vocabulary an external system owns.
2. One PO spans materials → item_no must sequence per PO (UQ sap_po_no+item_no).
3. station_devices.registered_by stays NOT NULL — seed registers devices as admin.
Deferred in-packet: vendor-master full parse (10k rows, fragile layout — pilot needs ~10 vendors,
real format arrives with SFTP); sale-report parser (needed for dashboard fixtures, owns: P32/P35).
Checklist: tests-first ✓ · scope ✓ · invariants ✓ · self-review ✓
Handoff: on dev machine `make seed` against Postgres after baseline migration; same counts expected.
