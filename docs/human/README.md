# Human docs index
- getting-started.md (P44): run the system, create users, reset pilot data.
- modules/ — one guide per domain as it's built (what it does, which screens, which rules).
- runbook.md (P44): deploy, backup/restore, SFTP failure recovery, common errors.
Planning docs live in docs/planning/.
